"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.saveToken = saveToken;
exports.getToken = getToken;
exports.localStorageKeyName = void 0;
var localStorage = global.window.localStorage;
var localStorageKeyName = 'token';
exports.localStorageKeyName = localStorageKeyName;

function saveToken(token) {
  var storage = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : localStorage;

  if (!token) {
    storage.removeItem(localStorageKeyName);
  } else {
    storage.setItem(localStorageKeyName, token);
  }
}

function getToken() {
  var storage = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : localStorage;
  return storage.getItem(localStorageKeyName);
}